{
  "name": "pomodoro-timer-settings",
  "version": "1.0.0",
  "private": true,
  "description": "pomodoro-timer アプリの settings 機能（feature_id: settings）。contract.yaml 参照。",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1"
  }
}
