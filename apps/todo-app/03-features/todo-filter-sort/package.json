{
  "name": "todo-filter-sort",
  "version": "1.0.0",
  "private": true,
  "description": "TodoItem 配列の完了状態・期限・優先度による絞り込み/並び替え機能（UIコントロール含む）",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
