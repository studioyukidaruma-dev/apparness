{
  "name": "@todo-app/assembly",
  "version": "1.0.0",
  "private": true,
  "description": "todo-app の組み上げ層。03-features/* の各機能を architecture.machine.yaml の interfaces[] に従って結線するだけで、機能内部のロジックは実装しない。",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
